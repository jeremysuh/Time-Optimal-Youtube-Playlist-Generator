import { createTheme } from "@material-ui/core";

export const AppTheme = createTheme({
    palette: {
        primary: {
            main: "#e53935",
        },
        secondary: {
            main: `#5c6bc0`,
        },
    },
});